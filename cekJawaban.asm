cekJawabanKuliner1:
    mov eax,dword[bandingNilai1]
    mov bl,byte[kunciKuliner+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
    
ret

cekJawabanKuliner2:
    mov eax,dword[bandingNilai2]
    mov bl,byte[kunciKuliner+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanKuliner3:
    mov eax,dword[bandingNilai3]
    mov bl,byte[kunciKuliner+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanBukuDanKata1:
    mov eax,dword[bandingNilai1]
    mov bl,byte[kunciBukuDanKata+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanBukuDanKata2:
    mov eax,dword[bandingNilai2]
    mov bl,byte[kunciBukuDanKata+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanBukuDanKata3:
    mov eax,dword[bandingNilai3]
    mov bl,byte[kunciBukuDanKata+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanTeknologi1:
    mov eax,dword[bandingNilai1]
    mov bl,byte[kunciTeknologi+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanTeknologi2:
    mov eax,dword[bandingNilai2]
    mov bl,byte[kunciTeknologi+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanTeknologi3:
    mov eax,dword[bandingNilai3]
    mov bl,byte[kunciTeknologi+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanVideoGame1:
    mov eax,dword[bandingNilai1]
    mov bl,byte[kunciVideoGame+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanVideoGame2:
    mov eax,dword[bandingNilai2]
    mov bl,byte[kunciVideoGame+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanVideoGame3:
    mov eax,dword[bandingNilai3]
    mov bl,byte[kunciVideoGame+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanFloraDanFauna1:
    mov eax,dword[bandingNilai1]
    mov bl,byte[kunciFloraDanFauna+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanFloraDanFauna2:
    mov eax,dword[bandingNilai2]
    mov bl,byte[kunciFloraDanFauna+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret

cekJawabanFloraDanFauna3:
    mov eax,dword[bandingNilai3]
    mov bl,byte[kunciFloraDanFauna+eax]
    ;cek apakah bener?
    mov al,byte[jawaban]
    cmp al,bl
ret